#include <iostream>
#include <stdlib.h>

using std::cout;
using std::endl;

void funcion(int N);
float x;
int i;
float ii;

int main(){
    drand(48);
    numero = rand();
    for (i=0;i=1000; i++){
        funcion(1000);
        algo = i + drand(48);
        algo2 = min(1,y)/ii;
        
        if(alfa<algo2){
            alfa = numero;
        } i++;
        
        cout << ii << " " << endl;
    }
    return 0;
}

void funcion(int N){   

    float y (float x){
        float x = exp(-x*x/2);
        if ( (x > 3.14) | (x<0)){
            y = 0;
        }else {
        ii = ( x > 3.14) | ( x < 0);
        ii = 0;
        }
    }
}       

